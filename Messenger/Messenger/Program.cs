using System;
using System.IO;

namespace Messenger
{
    class Program
    {
        static void Main(string[] args)
        {
            StreamReader reader = new StreamReader("auth.txt");
            //Load Settings
            string accountSid;
            string authToken;
            
        }
    }
}